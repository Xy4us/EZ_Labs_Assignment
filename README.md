/\*\*

- Header
- -logo
- -Nav items
- Body
- -search
- -Restaurant Container
- -Restaurant card
- -Img
- -name of the restaurant, star rating, cuisine, delivery time
- Footer
- -Copyright
- -links
- -Address
- -Contact
  \*/

There are two types of Export/Import

- Default
  export default (name of variable)
  import component from "path"

- Named Export/Import
  export const data;
  import {component} from "path"

# React Hooks

(Normal JS Utility Functions)

- useState() - generate superpowerful State variables in react. Whenever a state variable is upadte, react will re-render the component
- useEffect()

# 2 Types of Routing in Web App

- Client Side Routing - It's already loads the component and do not make network calls
- Server Side Routing - It's basically calls the api and get data from server side and reloads the website

# Redux ToolKit

- Install @reduxjs/toolkit and react-redux
- Build Store
- Connect our store to our app
- Build Slice
- Slice (cartSlice)
- Dispatch(action)
- Selector
